{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "permissions": {
    "allow": [
      "WebSearch",
      "WebFetch"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '<<\\s*EOF|cat\\s+>' && { echo 'BLOCKED: Do not use Bash heredocs or redirects to create files. Use the Write tool instead.' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ]
  }
}
